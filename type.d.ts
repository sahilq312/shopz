type User = {
    email : string,
    password : string
}

type Product = {
    id ?: id
    title : string,
    image : string,
    price : number,
    description? : string
    category? : string
}
